CACHE MANIFEST
# <%= (new Date()).toISOString() %>

CACHE:
<% assets.forEach(asset => { %><%= asset %>
<% }) %>

FALLBACK:
<% Object.keys(fallback).forEach(asset => { %><%= asset %> <%= fallback[asset] %>
<% }) %>

NETWORK:
*
http://*
https://*
